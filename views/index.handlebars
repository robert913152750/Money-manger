總金額: {{{totall}}}
<ul>
  {{#each records}}
  <li>
    {{{this.name}}}
    日期{{{this.date}}}
    金額{{{this.amount}}}
    <button><a href="expense/{{{this._id}}}/edit">修改</a></button>
    <button><a href="expense/{{{this._id}}}/delete">刪除</a></button>
  </li>
  {{/each}}
</ul>

<a href="/expense/new">
  <button type="button" class="btn btn-outline-dark">新增支出</button>
</a>